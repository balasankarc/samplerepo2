image: "alpine:latest"

.branch_build: &branch_build
  script:
    - JOB_NAME=( $CI_JOB_NAME )
    - echo "${JOB_NAME}"
    - echo "${JOB_NAME[0]}"
    - echo "${JOB_NAME[1]}"
    
.tag_build: &tag_build
  script:
    - echo "${CI_JOB_NAME}"

ubuntu_16.04 branch: *branch_build
ubuntu_16.04: *tag_build